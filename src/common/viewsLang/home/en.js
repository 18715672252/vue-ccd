module.exports = {
    // simple: 'Simple Request',
    tabs:{
        page:'Page description',
        component:'CNAP-UI',
    },
    commodity: 'Commodity',
    order: 'Order',
    text0: 'Send a request. The back end of the request does not need to query the database, but directly returns a simple string.',
    text1: 'Send requests. The back end of these requests queries the database normally and returns data.',
    text2: 'Send requests, the back-end of these requests cache the data from the database for the first time, and the subsequent same requests will not query the database.',
}